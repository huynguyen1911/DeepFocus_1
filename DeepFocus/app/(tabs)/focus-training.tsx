import { Redirect } from 'expo-router';

/**
 * Focus Training Tab Entry Point
 * Redirects to the main focus training screen
 */
export default function FocusTrainingTab() {
  return <Redirect href="/focus-training" />;
}
